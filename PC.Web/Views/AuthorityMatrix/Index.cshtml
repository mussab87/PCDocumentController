@model IEnumerable<PC.Services.Core.Models.AuthorityMatrix>
@{
    ViewBag.Title = "Authority Matrix List";
}

<section class="content">
    <div id="PlaceHolderHereUser"></div>
    <div class="container-fluid">
        <div class="block-header">

            <a asp-action="Create" asp-controller="AuthorityMatrix" class="btn bg-primary btn-lg btn-block runbtn" style="width: 200px" type="button">
                <i class="glyphicon glyphicon-work"></i>
                Add New Matrix
            </a>
        </div>

        <!-- Exportable Table -->
        <div class="row clearfix">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <div class="card">
                    <div class="header">
                        <h2>
                            Authority Matrix List
                        </h2>

                    </div>

                    <div class="body">

                        <div class="table-responsive">
                            @if (Model.Any())
                            {
                                <table class="table table-bordered table-striped table-hover dataTable js-exportable">
                                    <thead>
                                        <tr>
                                            <th>
                                                Id
                                            </th>
                                            <th>
                                                Authority Matrix Name
                                            </th>
                                            <th>
                                                Created By
                                            </th>
                                            <th>
                                                Created Date
                                            </th>
                                            <th>Updated By</th>
                                            <th>Updated Date</th>
                                            <th></th>

                                        </tr>
                                    </thead>
                                    <tfoot>
                                        <tr>
                                            <th>
                                                Id
                                            </th>
                                            <th>
                                                Authority Matrix Name
                                            </th>
                                            <th>
                                                Created By
                                            </th>
                                            <th>
                                                Created Date
                                            </th>
                                            <th>Updated By</th>
                                            <th>Updated Date</th>
                                            <th></th>

                                        </tr>
                                    </tfoot>
                                    <tbody>
                                        @foreach (var authority in Model)
                                        {
                                            <tr>
                                                <td>@authority.AuthorityId</td>
                                                <td>
                                                    @authority.Name
                                                </td>
                                                <td>
                                                    @authority.CreatedBy
                                                </td>
                                                <td>
                                                    @authority.CreatedDateTime
                                                </td>
                                                <td>
                                                    @if (authority.UpdatedBy != null)
                                                    {
                                                        @authority.UpdatedBy
                                                    }

                                                </td>
                                                <td>
                                                    @if (authority.UpdatedDateTime != null)
                                                    {
                                                        @authority.UpdatedDateTime
                                                    }

                                                </td>
                                                <td>
                                                    <form method="post" asp-action="Delete" asp-route-id="@authority.AuthorityId">

                                                        <a asp-action="Edit"
                                                   asp-route-id="@authority.AuthorityId" class="btn btn-info text-white" style="cursor:pointer">
                                                            <span class="glyphicon glyphicon-pencil"></span>
                                                        </a>
                                                        <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delet? : @authority.Name')">
                                                            <span class="glyphicon glyphicon-trash"></span>
                                                        </button>
                                                    </form>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            }
                            else
                            {
                                <p><h4 style="text-align:center">No Data to Display</h4></p>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- #END# Exportable Table -->
    </div>
</section>

<!-- Jquery Core Js -->
<script src="~/styles/ww/plugins/jquery/jquery.min.js"></script>

<!-- Custom Js -->
<script src="~/styles/ww/js/pages/tables/jquery-datatable.js"></script>
